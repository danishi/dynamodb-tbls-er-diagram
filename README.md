# dynamodb-tbls-er-diagram
DynamoDB ER Diagram with tbls

## Getting started
Configure the AWS CLI and set it up to access DynamoDB.

```bash
$ tbls doc
```

ER diagram and table definition are output.
